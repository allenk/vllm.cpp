#version 450
// vt::MatmulBT at DECODE, the GEMV tactic -- the PORTABLE 128-invocation module.
// Body, rationale and the variant rule are in vt_matmul_vec_body.glsl.
#extension GL_GOOGLE_include_directive : require
layout(local_size_x = 128, local_size_y = 1, local_size_z = 1) in;
#include "vt_matmul_vec_body.glsl"
